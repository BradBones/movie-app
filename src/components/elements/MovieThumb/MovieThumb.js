import React from 'react';
import './MovieThumb.css';

// Stateless component
const MovieThumb = (props) => {
    return (
        <div>
            MovieThumb
        </div>
    )
}

export default MovieThumb;